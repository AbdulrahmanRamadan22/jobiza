import 'package:flutter/widgets.dart';

class ImageResetPassword extends StatelessWidget {
  const ImageResetPassword({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/image_reser_pass1.png',
    );
  }
}
